/// Copy this file to `secrets.dart` and fill in your Supabase credentials.
/// `secrets.dart` is gitignored — never commit real keys.
class Secrets {
  static const supabaseUrl = '';
  static const supabaseAnonKey = '';
}
